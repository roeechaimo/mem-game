---
description: Initialize a feature by creating a dedicated folder, spec, plan, tasks, and an IDE custom command. (run as @technical-lead)
---

Act as the `@technical-lead` subagent and follow the step-by-step workflow defined in `.cursor/skills/spec-driven-flow/SKILL.md`.
